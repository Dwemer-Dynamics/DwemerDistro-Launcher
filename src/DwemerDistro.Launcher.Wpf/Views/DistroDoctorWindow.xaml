<Window x:Class="DwemerDistro.Launcher.Wpf.Views.DistroDoctorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Distro Doctor"
        Icon="/Assets/DwemerDistro.ico"
        Width="920"
        Height="720"
        MinWidth="760"
        MinHeight="560"
        WindowStartupLocation="CenterOwner"
        Background="#2C2C2C"
        Closing="Window_Closing">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#5E0505" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#7A2828" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Margin" Value="10,0,0,0" />
            <Setter Property="FontFamily" Value="Trebuchet MS" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="MinWidth" Value="120" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.9" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.8" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#404040" />
            <Setter Property="BorderBrush" Value="#565656" />
        </Style>

        <Style x:Key="ModeChoice" TargetType="RadioButton">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="#262626" />
            <Setter Property="BorderBrush" Value="#565656" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontFamily" Value="Trebuchet MS" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Padding" Value="14,10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="ModeBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="ModeBorder" Property="Background" Value="#5E0505" />
                                <Setter TargetName="ModeBorder" Property="BorderBrush" Value="#8A3030" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ModeBorder" Property="BorderBrush" Value="#888888" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ModeBorder" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Padding="14"
                Background="#1E1E1E"
                BorderBrush="#565656"
                BorderThickness="1"
                CornerRadius="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Image Grid.Column="0"
                       Source="/Assets/DwemerDistro.png"
                       Width="42"
                       Height="42"
                       Stretch="Uniform"
                       Margin="0,0,12,0" />

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Distro Doctor"
                               Foreground="White"
                               FontFamily="Trebuchet MS"
                               FontSize="20"
                               FontWeight="Bold" />
                    <TextBlock Text="Check DwemerDistro services, permissions, databases, ports, and recent logs."
                               Foreground="#CCCCCC"
                               FontFamily="Trebuchet MS"
                               Margin="0,4,0,0" />
                </StackPanel>

                <Border Grid.Column="2"
                        Background="{Binding StatusBrush}"
                        CornerRadius="13"
                        Padding="12,5"
                        VerticalAlignment="Center">
                    <TextBlock Text="{Binding StatusText}"
                               Foreground="White"
                               FontFamily="Trebuchet MS"
                               FontSize="11"
                               FontWeight="Bold" />
                </Border>
            </Grid>
        </Border>

        <Border Grid.Row="1"
                Margin="0,12,0,0"
                Padding="14"
                Background="#262626"
                BorderBrush="#565656"
                BorderThickness="1"
                CornerRadius="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <RadioButton Grid.Column="0"
                             Content="Check Only"
                             GroupName="DoctorMode"
                             IsChecked="{Binding IsCheckOnlyMode}"
                             IsEnabled="{Binding CanClose}"
                             Style="{StaticResource ModeChoice}"
                             Width="150" />
                <RadioButton Grid.Column="1"
                             Content="Check and Repair"
                             GroupName="DoctorMode"
                             IsChecked="{Binding IsRepairMode}"
                             IsEnabled="{Binding CanClose}"
                             Style="{StaticResource ModeChoice}"
                             Width="170"
                             Margin="10,0,0,0" />

                <TextBlock Grid.Column="2"
                           Text="Repair mode can restart services and apply guarded repairs. Keep this window open until it finishes."
                           Foreground="#CCCCCC"
                           FontFamily="Trebuchet MS"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"
                           Margin="16,0" />

                <Button Grid.Column="3"
                        Content="Run Doctor"
                        Command="{Binding RunCommand}"
                        Style="{StaticResource ActionButton}" />
            </Grid>
        </Border>

        <ProgressBar Grid.Row="2"
                     Height="6"
                     Margin="0,10,0,0"
                     IsIndeterminate="True"
                     Foreground="#FFD700"
                     Background="#1E1E1E"
                     BorderThickness="0"
                     Visibility="{Binding IsRunning, Converter={StaticResource BoolToVisibilityConverter}}" />

        <Border Grid.Row="3"
                Margin="0,10,0,0"
                Background="#151515"
                BorderBrush="#565656"
                BorderThickness="1"
                CornerRadius="6">
            <TextBox x:Name="LogTextBox"
                     Text="{Binding LogText, Mode=OneWay}"
                     TextChanged="LogTextBox_TextChanged"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     AcceptsTab="True"
                     TextWrapping="NoWrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     Background="Transparent"
                     Foreground="#E6E6E6"
                     BorderThickness="0"
                     Padding="12"
                     FontFamily="Consolas"
                     FontSize="12" />
        </Border>

        <Grid Grid.Row="4" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="Downloaded logs are saved to Desktop\DwemerDistro-Diagnostics."
                       Foreground="#AAAAAA"
                       FontFamily="Trebuchet MS"
                       VerticalAlignment="Center" />

            <Button Grid.Column="1"
                    Content="{Binding DownloadButtonText}"
                    Command="{Binding DownloadLogCommand}"
                    Style="{StaticResource SecondaryButton}" />
            <Button Grid.Column="2"
                    Content="Close"
                    IsEnabled="{Binding CanClose}"
                    Click="CloseButton_Click"
                    Style="{StaticResource SecondaryButton}" />
        </Grid>
    </Grid>
</Window>
